
apiVersion: apps/v1
kind: Deployment
metadata:
  name: supplier-service
  namespace: supplier-dev   # Ensure this namespace exists: kubectl create ns supplier-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: supplier-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: supplier-service
    spec:
      containers:
        - name: supplier-service
          image: myacrconnection.azurecr.io/supplier-service:latest  # ✅ Confirm ACR name matches your pipeline
          ports:
            - containerPort: 8081  # ✅ Ensure Spring Boot runs on 8081 (check application.properties)

          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "qa"

            - name: DB_HOST
              value: "your-postgres-host.postgres.database.azure.com"
            - name: DB_NAME
              value: "supplierdb"
            - name: DB_USER
              value: "postgres"
            - name: DB_PASSWORD
              value: "yourpassword"
            - name: DB_SSL
              value: "true"

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 15

          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "200m"
              memory: "256Mi"
