
apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: supplier-qa
spec:
  selector:
    app: supplier-service
  ports:
    - protocol: TCP
      port: 80        # External port
      targetPort: 8081 # Matches containerPort
  type: LoadBalancer  # ✅ Exposes service publicly
